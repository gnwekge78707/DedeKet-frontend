const http='http://localhost:8080'     //需要使用的ip1+接口共有部分
const glbhttp='http://localhost:8080'  //需要使用的ip2+接口共有部分
const osshttp="https://dedeket.oss-cn-hangzhou.aliyuncs.com/tmp/"
export default {    //将const创建的变量进行暴露，注意，暴露的必须是一个对象
    http,glbhttp, osshttp
}